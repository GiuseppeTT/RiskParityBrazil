library(targets)
library(tarchetypes)

tar_option_set(
    packages = "RiskParityBrazil",
    imports = "RiskParityBrazil",
    format = "qs",
    memory = "transient",
    garbage_collection = TRUE
)

list(
    # Empty
)
